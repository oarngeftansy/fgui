# Figma 插件测试包

此插件仅用于指定测试服务：`{{PUBLIC_ORIGIN}}`。

1. 解压 `Figma-to-FairyGUI-plugin.zip`。
2. Figma 打开 **Plugins > Development > Import plugin from manifest**。
3. 选择解压目录中的 `manifest.json`。

不要发布到 Figma Community。测试服务下线后，此插件将无法生成项目。

