# CLAUDE.md

## 持久记忆 —— 读它们、遵守它们

本项目把持久知识放在 `.claude/memory/`：

- `memory.md` —— 偏好与约定
- `wiki.md` —— 当前客观事实、验证结果和架构决策
- `learnings.md` —— 过程经验与踩坑记录（只追加，最新在上）

开始实质工作前先读这三个文件，并以代码和当前测试结果核实其中可能变化的事实。

每个实质任务结束前回写：

- 新踩坑追加到 `learnings.md`，不得改写历史；
- 新事实、结果、ID、配置更新到 `wiki.md`；
- 新的稳定偏好或项目约定更新到 `memory.md`；
- 没有可持久化内容时明确说明“无新记忆”。

