# FairyGUI 6.1.4 新建工程验收

## 范围

本验收覆盖通用 `Plan v2 → Manifest → FairyGUI XML → ZIP` 新建工程链路。不扫描既有
FairyGUI 工程，不执行 Project Binding。需求方组件映射仅作为通用语义输入；“村庄升阶”
仅是回归样例，生产 Writer 无页面名或节点 ID 特例。

## 自动化证据

- 通用工程 ZIP 以固定 SHA-256 作字节级 golden，并通过 ZIP CRC 与工程重开门。
- 安全回归覆盖 traversal/绝对路径、重复成员、case-fold 冲突、symlink 成员、
  XML DOCTYPE 与非法图片输入；失败时不发布产物。
- 村庄样例经同一套 normalize/semantic mapping/UIR/Plan API；因 UIR v1 尚不包含可生成
  组件定义树，它以 `fgui.component.definition_missing` 在 Writer 之前 fail closed。
- Task 10 新增验收集：`9 passed`；计划分层矩阵为 `181 passed`、`89 passed`、
  `174 passed, 2 skipped` 和 `5 passed`。
- 完整 Python 矩阵：`1061 passed, 4 skipped`；Ruff 通过；strict mypy 检查 55 个
  源码文件无错误；`git diff --check` 通过。
- 通用验收 ZIP：`bf62cd2789a7d0a44336e28a4c257d4fe91bee34c7673738bdf3f0662217c4ca`
  （1426 bytes）。

## FairyGUI Editor 6.1.4 门禁

为避免 GUI 焦点和多实例噪声，真实 Editor 门禁使用一个单一中立代表工程；
其他类型由 XML golden 与自动化 gate 覆盖。

Editor 操作：

1. 解压验证后的 ZIP。
2. 用 FairyGUI Editor 6.1.4 打开 `GenericWriterFixture/GenericWriterFixture.fairy`。
3. 观察是否出现 repair/migration 或其他 modal。
4. 保存、关闭、重开、再保存；对比两次保存前后的已声明文件哈希。

GUI 证据：

- 精确 Editor 版本：FairyGUI Editor 6.1.4。
- 验收产物：`.superpowers/sdd/task10-editor-gate/098b03d879e9a30814ec93407fc91e5cfa30bb48fa61d905334a2e1b1a97418e.zip`；
  SHA-256 为 `bf62cd2789a7d0a44336e28a4c257d4fe91bee34c7673738bdf3f0662217c4ca`。
- 工程：`.superpowers/sdd/task10-editor-gate/extracted/GenericWriterFixture/GenericWriterFixture.fairy`。
- 实际流程：打开 `GenericWriterFixture`，第一次 `Ctrl+S` 后关闭；重新打开，第二次
  `Ctrl+S` 后关闭。两轮后 FairyGUI 窗口数为 0，流程中未观察到 repair/migration
  或其他 modal。
- 两次保存后声明文件字节哈希均与保存前一致（`Match=true`）：
  - `GenericWriterFixture.fairy`：`bb74d97ef5039e471184ef3efeb3932e8e4843d5e856e56c88c2aec181ac92d8`
  - `assets/Generated/package.xml`：`0a90a6b2c18f6443344ec5462511bc12c27c748c079bcbc078dbf841c2d334d4`
  - `assets/Generated/components/root-6e07d820.xml`：`6a804a0c74f0f6ccd8fb1c8b60b0296b05cf62f1b7bbdb3236f4a3a781f08a6a`
  - `assets/Generated/resources/generic-pixel-7fb786de.png`：`4ff6ab670a58c14270e034e2090d9a432caa263a14e0a25785386b0c12f880b5`

本门禁只对上述单一中立代表工程给出真实 Editor 证据；其余对象类型仍由 XML
golden 和自动化 gate 覆盖。Unity/FairyGUI 窗口的 state/screenshot API 不受支持，
因此不将本记录表述为截图或完整 accessibility 审计。

## v1 保留边界

Project Binding、既有工程更新/资源复用、Controller/Gear/List 和没有自包含定义树的
组件引用均不在 Writer v1 范围内。
